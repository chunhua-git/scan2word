# scan2word 使用说明

**扫描件 PDF → 可编辑 Word（离线 · 免费）**

把扫描件 PDF 拖进来，自动识别文字并还原表格结构，输出可以直接改字的 `.docx`。
**全程离线，材料一个字节都不上传。**

---

## 一、怎么启动

1. 把 `scan2word` 整个文件夹解压到任意位置（桌面、D 盘都行，**不要只拷 exe**，它旁边的文件是一起的）。
2. 双击里面的 **`scan2word.exe`**。
3. 第一次打开会慢几秒（要加载识别模型），之后就快了。**不需要联网，不需要装 Python，不需要显卡。**

> 如果 Windows 弹出"已保护你的电脑"：点「更多信息」→「仍要运行」。
> 这是没有花钱买数字签名的正常提示，不是病毒。

---

## 二、怎么用（三步）

1. **拖进来**：把 PDF 文件（或整个文件夹）拖到窗口中间的列表里。也支持点「添加文件…」。
2. **选选项**（一般不用改，默认就是最好的）：
   | 选项 | 说明 |
   |---|---|
   | OCR 引擎 | 默认 RapidOCR，纯离线、中文强。装了 PaddleOCR 的机器可以换 PP-StructureV3 |
   | 扫描页渲染 DPI | 默认 300。**字特别小**可以调到 400；想更快调到 250 |
   | 同时输出双层可搜索 PDF | 勾上会多给一个 PDF：看着还是原件，但能全文搜索、能复制文字 |
   | 数字字段换分辨率复核 | 默认开。证件号、账号、编号这类关键数字会用第二个分辨率再认一遍，两次一致才算过 |
   | 忽略文字层，强制 OCR | 电子版 PDF 想走 OCR 时用（一般不用） |
   | 转换完成后打开输出文件夹 | 默认开 |
   | 输出文件夹 | 留空＝和源文件放一起，**文件名保持不变** |
3. **点「开始转换」**。转完自动打开输出文件夹。

---

## 三、输出是什么

每个 PDF 旁边会生成同名文件：

| 文件 | 用途 |
|---|---|
| `xxx.docx` | **主产物**。可直接改字、改抬头、改编号、当模板复用 |
| `xxx.质检报告.md` | **先看这个**。告诉你哪里需要人工核一眼 |
| `xxx_可搜索.pdf` | 勾选了才有。原件样子的双层 PDF，能全文搜索 |
| `xxx.txt` | 纯文本，方便快速核对 |

### 质检报告怎么看

- `✅` 通过 —— 例如「证件号校验通过」「两次识别一致」「与 PDF 原文逐字一致（错字率 0.00%）」
- `⚠️ 待核` —— **需要你扫一眼**。比如某个字置信度偏低、两次识别读数不一致、疑似形近字、编号里字母数字容易混
- `❌` 错误 —— 例如日期不存在、数字格式不合理。一定要处理

**"待核"不等于错，是提示你多看一眼。** 追求的是"错字率≈0 且剩余风险都可追溯"，不是"悄悄地放过"。

---

## 四、它保证什么

- **电子版 PDF（带文字层的）**：走"矢量还原"，逐字比对原文，**错字率 0.00%**，且速度是亚秒级。
- **扫描件（没文字层的）**：走离线 OCR。中文正文在 300dpi 下实测置信度普遍 ≥0.99。
- **结构保留**：表格栏线、左栏栏目名＋右栏内容、**合并单元格**（如左侧栏目名纵跨多行）、
  有框线和**无框线**的表格、**勾选框 □/☑**、**空白填写栏**、**印章 / 公章位置**（按原位置裁图嵌入）。
- **输出可直接交付**：Word 里全是真文字（不是图片），改完字直接另存 PDF 就能发出去。

---

## 五、常见问题

**Q：转出来个别的字不对怎么办？**
1. 先看 `质检报告`，`⚠️ 待核` 那几条就是重点。
2. 提高「渲染 DPI」到 400 重转一次——字小、扫描淡的文件最有效。
3. 原件歪了会自动纠偏，报告里会写"检测到倾斜 X°"。
4. 单号、编号里字母和数字容易混（比如 `O` 和 `0`）。这类**工具不会替你猜**，只在报告里列出来让你核对。

**Q：能处理带印章、有勾选框的文件吗？**
能。印章按原位置作为图片嵌入（保留红色），勾选框还原成 `□`/`☑` **字符**（可以直接改）。

**Q：空白模板（没填字的）能转吗？**
能，而且这是最稳的场景：空白栏会原样保留成空白单元格，可以直接往里填字当模板用。

**Q：表格一条线都没画，能还原成表格吗？**
能。靠"行对齐 + 空白分栏"推断，员工信息表、财务报表、报价单这类都能还原。
反过来，如果遇到双栏排版的文章（论文、说明书），工具不会把它误切成一堆短格。

**Q：为什么扫描页要十几秒一页？**
CPU 离线识别的正常水平。批量转换会排队依次处理，转完自动出结果，不用守着。
电子版 PDF 是亚秒级。

**Q：支持哪些输入？**
图片型扫描件 PDF、电子版 PDF，多页、多个文件、整个文件夹都支持。
一个 PDF 里两种页面混排也能处理（每页自动选路线）。

**Q：水印/页眉页脚能去掉吗？**
能。点「忽略区域…」，在页面预览上**拖框**圈住不想识别的区域，转换时会整块跳过。

**Q：材料安全吗？**
安全。**纯本地运行，程序不含任何联网上传代码**，识别模型就在程序文件夹里。
断网也能用，你可以拔网线试。

**Q：想给别的同事用？**
整个 `scan2word` 文件夹打包发过去，对方解压双击 `scan2word.exe` 即可，不需要装任何环境。

---

## 六、系统要求

- Windows 10 / 11（64 位）
- 内存 4GB 以上（8GB 更顺）
- **不需要独立显卡**，纯 CPU 运行
- 磁盘：程序约 300–600MB，转换输出按原文件大小

---

## 七、开源许可证（可自由使用、修改、闭源分发）

本项目**不含任何 AGPL 组件**，可以放心闭源分发：

| 组件 | 许可证 |
|---|---|
| RapidOCR（PP-OCRv4 模型） | Apache-2.0 |
| onnxruntime | MIT |
| pypdfium2（PDF 渲染） | Apache-2.0 / BSD-3-Clause |
| pdfplumber / pdfminer.six（文字层与线条） | MIT |
| reportlab（双层 PDF） | BSD-3-Clause |
| python-docx（Word 输出） | MIT |
| PySide6（界面，动态链接） | LGPL-3.0 |
| OpenCV | Apache-2.0 |

> 说明：早期版本用过 PyMuPDF，它是 **AGPL-3.0**，会把整个项目"传染"成 AGPL，
> 与"可闭源分发"冲突，因此已全部替换为上表中的宽松许可组件。
